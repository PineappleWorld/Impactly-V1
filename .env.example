# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Application
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Stripe (configured via admin dashboard - stored in app_settings table)
# stripe_secret_key
# stripe_webhook_secret

# Reloadly Gift Cards (configured via admin dashboard - stored in app_settings table)
# reloadly_client_id
# reloadly_client_secret

# Every.org (configured via admin dashboard - stored in app_settings table)
# every_org_api_key

# Pricing Configuration (configured via admin dashboard - stored in app_settings table)
# markup_percentage (default: 5)
# profit_split_company (default: 50)
# profit_split_charity (default: 50)
# impact_tickets_multiplier (default: 10)
